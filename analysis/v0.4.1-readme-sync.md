# v0.4.1-readme-sync

## Request

Synchronize the main `README.md` with the completed Experiment 4 free-`XYZ` BA results.

## Changes

- Added `run_exp4_experiment.py` and `outputs_exp4/` to the project overview in `README.md`.
- Added a full `Experiment 4` section covering:
  - motivation,
  - free-`XYZ` state definition,
  - local/global regime setup,
  - scene-figure interpretation,
  - summary-figure interpretation,
  - the practical difference between local BA and global BA in this experiment.
- Updated the reproduce commands to include `run_exp4_experiment.py`.
- Added version label `v0.4.1-readme-sync` to the README version table.

## Notes

- No experiment code or outputs changed in this version.
- This is a documentation synchronization pass so the repository README matches the already committed Experiment 4 implementation and figures.
